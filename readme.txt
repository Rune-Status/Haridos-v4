RuneCore v4

RuneCore v4 is a Fast and Object Orientated Server Application written in Java built to emulate RuneScape. The server uses a codec style plugin system to alternate between revisions.
The groovy scripting engine is also implemented to keep the content dynamic and improve customization of the server with ease


www.runecore.org